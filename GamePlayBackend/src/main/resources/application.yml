spring:
  application:
    name: gameplay
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/gameplay
    username: ---
    password: ---
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: false
security:
  jwt:
    token:
      secret-key: 53cr37
spring-doc:
  paths-to-match:
    - /**
  swagger-ui:
    use-root-path: true
